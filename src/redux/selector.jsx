

export const selectUsers = state => state.users.items.users;

export const selectIsLoading = state => state.users.isLoading;

export const selectPosition = state => state.users.positions;

export const selectError = state => state.users.error;

export const selectPage = state => state.users.page;

export const selectToken = state => state.users.token;